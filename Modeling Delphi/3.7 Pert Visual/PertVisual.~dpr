program PertVisual;

uses
  Forms,
  UPertVisual in 'UPertVisual.pas' {Form1},
  UPert in 'UPert.pas',
  USimulation in '..\USimulation\USimulation.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
